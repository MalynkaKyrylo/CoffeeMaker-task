package app;

import app.utils.AppView;
import app.utils.AppController;

public class App {
    public static void main(String[] args) {
        AppController.filterChoice(AppView.chooseOption());

    }
}
